<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>DBA</title>
        <style>
            .container {
                width: 80%;
                padding: 20px 0 0 10%;
            }
            .input, btn {
                border: 2px solid grey;
                padding: 5px;
            }
            .input, .results {
                width: 100%;
                margin-bottom: 5px;
            }
            .btn {
                margin-bottom: 10px;
                padding: 10px;
                width: 150px;
                background-color: #002166;
                color: #fff;
                border: none;
                cursor: pointer;
            }
            .results {
                overflow: scroll;
                height: 200px;
                border: 2px dashed grey;
                padding: 5px;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <form action="{{ action }}">
                <input type="hidden" name="csrf" value="{{ csrf }}" /><br/>
                <input type="password" autofocus="true" value="{{ key }}" placeholder="Key" name="key" class="input" /><br/>
                <textarea rows="10" placeholder="Sentence" name="sentence" class="input">{{ sentence }}</textarea><br/>
                <input type="submit" value="Submit" class="btn" />
                <div class="results">
                    {{ dump(results) }}
                </div>
            </form>
        </div>
    </body>
</html>
